"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const graphql_1 = require("graphql");
const fs_1 = require("fs");
async function loadFromGqlFile(filePath) {
    const content = fs_1.readFileSync(filePath, 'utf-8');
    if (content && content.trim() !== '') {
        if (/^\#.*import /i.test(content.trimLeft())) {
            const { importSchema } = await eval(`require('graphql-import')`);
            return graphql_1.parse(importSchema(filePath));
        }
        else {
            return graphql_1.parse(content);
        }
    }
    return null;
}
exports.loadFromGqlFile = loadFromGqlFile;
//# sourceMappingURL=load-from-gql-file.js.map